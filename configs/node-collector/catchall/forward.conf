  <match **>
    @type forward
    send_timeout 60s
    recover_wait 10s
    heartbeat_interval 1s
    phi_threshold 16
    hard_timeout 60s
    heartbeat_type tcp

    <server>
      name normalizer
      host fluentd.perf.lab.eng.bos.redhat.com
      port 4000
      weight 60
    </server>
  </match>
